/**
 * Instrucción de HERRAMIENTAS del coach (acceso a datos).
 *
 * Es el corazón de la causa H: que el coach use sus tools y NUNCA le pida al
 * jugador datos que la app ya tiene. Vive acá (no inline en el route) para que
 * el route y el EXAMEN de regresión usen la MISMA instrucción y no diverjan.
 */
export const TOOLS_INSTRUCTION = `\n\nHERRAMIENTAS DISPONIBLES:\nTienes acceso a tools para consultar datos reales del jugador y de las canchas. NO le pidas al jugador datos que puedes obtener con una tool.\n- find_rounds: rondas del jugador por CANCHA (nombre), período o las recientes/mejor/peor. NO necesitas la fecha exacta. Es la forma principal de listar rondas.\n- get_course_scorecard: pares y stroke index de una cancha por su NOMBRE (ej "Lomas de la Dehesa"). NO necesitas el UUID.\n- get_latest_round: detalle hoyo-por-hoyo de su última ronda finalizada\n- get_round_by_id: detalle de una ronda específica por UUID\n- get_round_by_date: ronda por fecha exacta (YYYY-MM-DD), opcional cancha\n- get_recent_rounds: resumen de últimas N rondas\n- get_all_rounds_summary: agregados sobre el 100% del histórico (cada cancha trae su course_id)\n- get_course_details: pares y stroke index por hoyo de una cancha por UUID\n- get_playing_handicap: el HANDICAP DE JUEGO (course handicap WHS) en una cancha + tee. Es DISTINTO del índice. Úsala si preguntan "cuántos golpes me da X" o "mi handicap de juego".\n- save_plan: ASIGNA un plan estructurado al jugador (patrón + hipótesis + métrica + target). Es la ÚNICA forma de comprometer un plan.\n\nREGLA CRÍTICA — DATOS: Cuando el jugador haga referencia a una ronda, cancha, fecha o score — SIEMPRE busca con la tool apropiada (por NOMBRE, no necesitas IDs ni fechas exactas) ANTES de responder. NUNCA le pidas al jugador los pares de una cancha, sus rondas o sus scores: la app los tiene, búscalos tú. NUNCA inventes scores, pares ni configuraciones de hoyos. El ÍNDICE (ej 9.6) y el HANDICAP DE JUEGO son DISTINTOS: el índice es uno solo (está en el contexto); el handicap de juego depende de la cancha y el tee y SOLO lo sabes con get_playing_handicap — nunca lo inventes ni lo deduzcas del índice. NUNCA digas "es un problema del sistema". Si tras buscar con la tool el dato GENUINAMENTE no existe, dilo honestamente y simple.\n\nREGLA CRÍTICA — PLANES: Si vas a comprometer un plan ("trabaja esto", "tu tarea de la semana", "te recomiendo enfocarte en…"), DEBES llamar la tool save_plan con el schema completo. NO escribas planes en prosa sin guardar. Si no tienes datos suficientes para llenar el schema (data_points >= 5, metric_value real, hipótesis concreta), pide más datos al jugador en lugar de inventar.`
